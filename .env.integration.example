# Integration Environment Variables (example)
# Copy this file to .env.integration and fill in real values or let CI inject secrets.

# --- DATABASE ---
# For CI/Docker environments, using individual POSTGRES_* variables is the required standard.
# The application will fail if both these and DATABASE_URL are set.
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_HOST=db
POSTGRES_PORT=5432

# DEPRECATED: Use the individual POSTGRES_* variables above.
# DB_HOST=db
# DB_PORT=5432
# DATABASE_URL=

# --- REDIS ---
REDIS_HOST=redis
REDIS_PORT=6379

# --- JWT ---
JWT_SECRET=

# --- SUPABASE ---
SUPABASE_URL=
SUPABASE_KEY=

# --- OTHER ---
NODE_ENV=integration
PORT=3000
CORS_ORIGIN=*

# --- App Configuration ---
AUTH_MODE=local

# --- Database Credentials & Connection ---
# DEPRECATED: These variables have been removed in favor of the POSTGRES_* standard.
# DB_USERNAME=
# DB_PASSWORD=
# DB_DATABASE=

# --- App Specific Settings ---
GEOHASH_PRECISION=7
VENUE_SCAN_RADIUS_METERS=5000
VENUE_SCAN_STALENESS_THRESHOLD_HOURS=720
VENUE_SCAN_WORKER_CONCURRENCY=3
VENUE_STALENESS_REFRESH_CRON='0 0 1 1 *'
MAX_UPLOAD_SIZE=10485760
IMAGE_PROCESSING_QUALITY=85
IMAGE_PROCESSING_ASYNC=true
STATIC_FILE_CACHE_TTL=86400
STATIC_FILE_ETAG_ENABLED=true 